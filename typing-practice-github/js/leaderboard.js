(async()=>{await I18N.load();I18N.nav("leaderboard");document.getElementById("app").innerHTML=`<main><div class="container"><section class="hero"><div class="kicker">Top 100</div><h1>${T.leaderTitle}</h1><p>${T.leaderDesc}</p></section><div id="msg"></div><section class="card"><div class="table-wrap"><table><thead><tr><th>${T.rank}</th><th>${T.username}</th><th>${T.country}</th><th>${T.wpm}</th></tr></thead><tbody id="rows"></tbody></table></div><div class="row between" style="margin-top:14px"><button id="prev" class="btn">←</button><strong id="page"></strong><button id="next" class="btn">→</button></div></section><section class="card" style="margin-top:18px"><div class="muted">Your position</div><div id="mine" class="stat">—</div></section></div></main><footer class="footer">Typing Practice</footer>`;let page=1,rows=[];async function load(){if(!db){document.getElementById("msg").innerHTML=`<div class="notice">Configure Supabase first.</div>`;render();return}let {data,error}=await db.from("scores").select("wpm,user_id,profiles(username,country)").order("wpm",{ascending:false}).limit(100);if(error){document.getElementById("msg").innerHTML=`<div class="error">${U.esc(error.message)}</div>`;return}rows=data||[];render();let {data:{user}}=await db.auth.getUser();if(user){let {data:s}=await db.from("scores").select("wpm").eq("user_id",user.id).order("wpm",{ascending:false}).limit(1);if(s?.[0]){let {count}=await db.from("scores").select("id",{count:"exact",head:true}).gt("wpm",s[0].wpm);document.getElementById("mine").textContent=((count||0)+1)<=100?(count+1):"100+"}}}function render(){let st=(page-1)*20;document.getElementById("rows").innerHTML=rows.slice(st,st+20).map((r,i)=>`<tr><td>#${st+i+1}</td><td>${U.esc(r.profiles?.username||"Guest")}</td><td>${U.flag(r.profiles?.country)} ${U.esc(r.profiles?.country||"—")}</td><td><strong>${Number(r.wpm).toFixed(1)}</strong></td></tr>`).join("")||`<tr><td colspan="4" class="muted">No scores yet.</td></tr>`;document.getElementById("page").textContent=`${T.page||"Page"} ${page}/5`;document.getElementById("prev").disabled=page===1;document.getElementById("next").disabled=page===5}document.getElementById("prev").onclick=()=>{if(page>1){page--;render()}};document.getElementById("next").onclick=()=>{if(page<5){page++;render()}};load()})();
